# mandarin_notes_api (Ongoing)
A simple Python API (using FastAPI) that accepts data from your frontend and saves your notes — pinyin, hanzi, English translation, Malay translation — into a local JSON or CSV file.
